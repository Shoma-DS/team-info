# Website Clone Workspace

This project was initialized from the team-info `clone-website` template.

## Quick Start

```bash
source "$HOME/.nvm/nvm.sh"
nvm use 24
npm install
npm run build
```

After the workspace builds, use the team-info `clone-website` skill to reverse-engineer the target site into this project.
