# linux-x86_64 artifacts

まだビルド済みバイナリがありません。**Linux で初めて `setup` を実行した人**が
`setup/headroom/install.sh` 経由でビルドし、ここに wheel が生成されます（要 Rust）。

> Linux は `ort` の公式 x86_64 バイナリを使う通常ビルドのため、パッチや外部 ONNX は不要。**未検証**。
